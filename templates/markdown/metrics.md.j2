{% import "_macros.j2" as macros %}
# Metrics: `{{ ctx.root_namespace }}`

This document describes the `{{ ctx.root_namespace }}` metrics.

{% for metric in ctx.metrics | sort(attribute="name") %}
## `{{ metric.name }}` {{ macros.stability_badge(metric.stability, metric.deprecated) }}

{{ metric.brief | trim }}

{% if metric.note %}{{ metric.note | trim }}{% endif %}

**Instrument**: `{{ metric.instrument }}`
**Unit**: `{{ metric.unit }}`

{% if metric.attributes %}
### Attributes

| Attribute | Type | Requirement Level | Description |
|-----------|------|-------------------|-------------|
{% for attr in metric.attributes | attribute_sort %}| `{{ attr.key }}` | {{ macros.attribute_type(attr) }} | {{ macros.requirement_level(attr.requirement_level) }} | {{ macros.brief_inline(attr.brief) }} |
{% endfor %}

{% endif %}
{% endfor %}
